name: frontend

on:
  push:
    paths:
      - 'frontend/**'
      - 'Dockerfile'
      - '.github/workflows/frontend.yml'
  pull_request:
    paths:
      - 'frontend/**'
      - 'Dockerfile'
      - '.github/workflows/frontend.yml'

jobs:
  ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          # jsdom 30 (via vitest) requires Node >= 22.22 — undici 8 needs
          # worker_threads.markAsUncloneable (Node 20/early-22 crash workers)
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run ci

  # Keep the split Dockerfiles honest: builds both containers on every
  # frontend change (backend image + frontend nginx image via compose).
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker compose build
