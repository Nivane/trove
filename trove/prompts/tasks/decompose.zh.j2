判断下面的用户问题是否是一个包含多个独立子问题的多任务指令。

多任务判定标准(满足其一):
- 用序号明确列出多项:"1) 2) 3)"、"一、二、三"、"①②③"等
- 用"依次/分别/先…再…/还要/以及/同时…另外"等连接多个分析目标
- 请求中包含多个彼此独立的分析问题

反例(不算多任务):单一问题但描述较长、带一个时间范围、只列了一个分析维度。

输出格式:只输出一个 JSON 对象,不要任何其他文字。
- 多任务:{"tasks": ["子问题1(必须是一个完整的可独立查询的问题)", "子问题2", ...]}
- 单任务:{"tasks": []}

用户问题:
{{ question }}
