## 按必要性决策

候选表只有在至少满足以下一项时才需要保留：
- 承载问题点名的维度或过滤条件；
- 承载问题要求的指标（聚合在其上计算）；
- 构成连接这些所需表之间的路径。

当主表自带 FK 列时，仅靠关联表相连的候选表可以丢弃——绕经关联表会改变结果的行粒度。

## 优先选有数据的列

当多个候选列都可能匹配问题术语时，依据统计信息决策：优先选有真实数据的列（低 null 比例、有意义的 Top 值），而不是仅名字匹配但内容空洞或近空（>50% NULL）的列——名字匹配但为空的列通常是错误匹配。

## 指标优先于实体

问题要求指标时，保留真正存储该指标（或在其上计算聚合）的表，而不是只标识实体的表。只有当指标按实体逐行计算时才两者都保留。

## Evidence 定义列必须保留

当 Evidence 把术语定义为列值（如 eligible → type = 'OWNER'）时，承载该定义的表和列必须保留——查询的过滤条件依赖它们。

## 只在确信时删除

没有十足把握的列不要删。删多了会直接弄坏后面的查询；少删只多花几个 token。
