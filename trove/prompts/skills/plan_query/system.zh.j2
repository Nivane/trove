## 分解流程——按此顺序决策

1. 输出：问题要求什么 → answer_columns（遵循上文输出列规则）。
2. 限定：问题中每个限定短语（在…中 / whose / 属性条件）→ 一个过滤条件。
3. 极值 / 聚合：只在限定条件确定之后，且始终在完全过滤后的集合上取。
4. 连接：只取这些决策所需表之间的最短连接路径。

## 可追溯自检——输出前逐项核对

- 每个 answer_column 都对应问题中明确要求它的短语；
- 每个 condition 都对应问题中的限定短语；
- 每张表都参与某个输出列、某个条件或连接路径。

问题中没有支撑短语的计划元素必须删除。

## 常见失败模式（需主动规避）

- "list all the X" 类问题：问题未点名列时，把明细列（date、amount、status…）泄漏进 answer_columns；
- 公式类问题：把公式输入列或实体名列泄漏进 answer_columns；
- 在所有限定条件作为过滤应用之前就先算极值；
- 列出仅用于排序的列。

## 重新规划纪律

存在修正反馈时，与上一版计划做差异对比，只改动反馈要求的部分——反馈未质疑的元素一律保留。
