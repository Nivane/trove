## 诊断流程

1. 先读失败类别：规则族命中（F1 形状 / F2 过滤 / F3 值域 / F4 排序）、执行错误、投票分歧、或 reflect 的 RETRY 判定。
2. 把错误类型映射到真正的故障层——修掉它才能消除根因的那一层，而不是症状浮现的那一层。
3. 最后再写修正方案，直指根因。

## 如何利用 [Regression check]（回归检查）反馈

- "Invalid fix"（结果集与上一版完全相同）：上一轮复现了同一错误——必须换思路，不要换措辞重述。
- "no progress"（同一规则族仍在违反）：修复打错了维度——找到仍被违反规则背后的问题短语，针对它修。
- "problem shift"（出现新规则族违规）：修 A 引入了 B——检查改动，两个规则都要兼顾；修复不能破坏已经通过的部分。

## 失败模式诊断

- 空结果 / 零行：过滤条件过严或 join 键错误——先放宽或去掉最不确定的条件，再核对 join 列。
- 语法错误：简化写法——避免 CTE、UNION 和深层嵌套，用直白的 SELECT+JOIN。
- 超时：减少参与的表、缩小过滤范围；绝不允许笛卡尔积。
- 候选结果不一致：选择与问题措辞最吻合的解释。
- F2 过滤规则命中：问题意图没被理解——需要语义级重写条件（revisor），而不是实现级的表面修补。

## 回退纪律

- 选真正的根因层：SQL 本身写错→gen_sql；查询计划/聚合思路错→query_sketch；表/列匹配错或漏表→schema_linking。
- 重复的回退目标会沿阶梯升档——贪图方便而选错层，会白白烧掉重试预算。
