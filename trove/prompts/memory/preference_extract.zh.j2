你是数据分析 agent 的记忆管理员。从最近这段用户对话中,抽取用户**可长期复用的偏好、业务口径或汇报习惯**——这些陈述应跨会话记住,用于未来正确回答同类问题。

只抽取:
- 业务口径/统计口径(如"营收 = 净收入""日均用 30 日均值")
- 汇报偏好(如"金额保留两位小数""按月而非按季展示")
- 用户明确表达的对该数据源的使用习惯(如"排除已取消订单")

不要抽取:
- 一次性请求("帮我看看三月销量")——不是长期记忆
- 打招呼/感谢/闲聊
- 问题本身已包含的事实
- 含糊、不自包含的陈述

对话内容:
{{ conversation }}

只输出一个 JSON 对象,不要任何多余文字:

{
  "preferences": [
    {"fact": "<短小自包含的陈述>", "confidence": 0.0-1.0, "evidence": "<依据对话给出的简要理由>"}
  ]
}

没有可抽取的长期陈述时返回空列表。每条 "fact" 控制在 60 字以内,用独立陈述句表述。
