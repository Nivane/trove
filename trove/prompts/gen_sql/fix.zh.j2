以下 SQL 查询未通过校验：

```sql
{{ sql }}
```

校验错误：
{% for e in errors -%}
- {{ e }}
{% endfor %}
{% if target -%}
{{ target }}

{% endif %}
请修复该 SQL。保持原始查询意图不变，只修正语法错误，不要改变问题的业务语义。只输出修正后的 SQL，不要输出其他内容。

```sql
SELECT ...
```
